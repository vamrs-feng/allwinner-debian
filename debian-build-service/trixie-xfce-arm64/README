* To build binary image:
  * install live-build qemu-user-static boostrap
  * run configure
  * run make

* Scripts:
  * configure
  * Makefile

* Configuration Layout

`-- ./config

* Customization Layout

`-- ./customization
